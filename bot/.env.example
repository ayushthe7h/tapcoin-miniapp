BOT_TOKEN=123456:ABC-your-telegram-bot-token
WEBAPP_URL=https://yourdomain.com
# Optional — if unset, the bot looks this up via getMe() on first use.
# Must match the bot's Main Mini App configuration in @BotFather for
# referral links (https://t.me/<username>?startapp=CODE) to work.
BOT_USERNAME=YourBot
